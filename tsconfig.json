{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "rootDir": "./", // Set rootDir to project root to include both src and tests
    "outDir": "./dist",
    "esModuleInterop": true,
    "strict": true
  },
  "include": ["src", "tests"] // Explicitly include both src and tests folders
}